;;; ../../home-config/emacs_configs/doom/config-dita.el -*- lexical-binding: t; -*-


;; ditaa exporting
;; (org-babel-do-load-languages
;;  'org-babel-load-languages
;;  '(
;;    (emacs-lisp . t)
;;    (plantuml . t)
;;    (dot . t)
;;    )
;;  )

;; (setq org-ditaa-jar-path "/usr/share/ditaa/ditaa.jar")
