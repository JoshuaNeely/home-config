return {
  {
    "saghen/blink.cmp",
    opts = {
      completion = {
        menu = {
          -- Delays the completion menu when typing
          auto_show_delay_ms = 500,
        },
      },
      signature = {
        -- Delays the function signature help pop-up
        auto_show_delay_ms = 500,
      },
    },
  },
}
